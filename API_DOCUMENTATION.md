# API接口文档

**版本**：v1.0.0  
**最后更新**：2025-12-20

---

## 📋 目录

1. [基础说明](#基础说明)
2. [鉴权机制](#鉴权机制)
3. [接口列表](#接口列表)
4. [错误码说明](#错误码说明)
5. [限流说明](#限流说明)

---

## 基础说明

### API基础URL

```
开发环境：http://localhost:8000/index.php/api
生产环境：https://your-domain.com/index.php/api
```

### 响应格式

所有接口统一返回JSON格式：

**成功响应**：
```json
{
  "code": 1,
  "msg": "操作成功",
  "time": 1766240433,
  "data": {
    // 具体数据
  }
}
```

**错误响应**：
```json
{
  "code": 0,
  "msg": "错误信息",
  "time": 1766240433,
  "data": null
}
```

### 请求方式

所有接口使用 `GET` 方法，参数通过Query参数传递。

---

## 鉴权机制

### 基本规则

所有API接口（除健康检查接口外）都需要在Query参数中携带 `api_key` 参数。

**鉴权规则**：
- ❌ 不带 `api_key` 参数：返回 400 错误（Missing required parameter: api_key）
- ❌ `api_key` 无效：返回 401 错误（Invalid api_key）
- ❌ 批次被禁用：返回 403 错误（Batch is disabled）
- ✅ 验证通过：继续执行接口逻辑

### 测试密钥

```
test_api_key_123456
```

---

## 接口列表

### 1. 健康检查接口

**接口路径**：`GET /api/health/index`

**功能描述**：检查系统运行状态，用于监控和健康检查

**请求参数**：
- `api_key`（可选）：批次密钥。如果提供会验证并返回批次信息

**响应示例**：
```json
{
  "code": 1,
  "msg": "Health check completed",
  "time": 1766240433,
  "data": {
    "status": "ok",
    "timestamp": 1766240433,
    "version": "1.0.0",
    "database": "connected",
    "cache": "working",
    "batch": {
      "id": 1,
      "name": "测试批次",
      "status": 1
    }
  }
}
```

**响应字段说明**：
- `status`：系统状态（"ok"表示正常，"error"表示异常）
- `timestamp`：当前时间戳（Unix时间戳）
- `version`：系统版本号
- `database`：数据库连接状态（"connected"表示已连接）
- `cache`：缓存状态（"working"表示正常工作）
- `batch`：批次信息（如果提供了api_key且验证通过）

**测试命令**：
```bash
# 不带api_key
curl "http://localhost:8000/index.php/api/health/index"

# 带api_key
curl "http://localhost:8000/index.php/api/health/index?api_key=test_api_key_123456"
```

---

### 2. 币种列表接口

**接口路径**：`GET /api/coins/list`

**功能描述**：获取所有启用的币种列表

**请求参数**：
- `api_key`（必需）：批次密钥

**响应示例**：
```json
{
  "code": 1,
  "msg": "获取成功",
  "time": 1766240433,
  "data": [
    {
      "id": "bitcoin",
      "symbol": "btc",
      "name": "Bitcoin",
      "logo": "http://localhost:8000/uploads/coins/bitcoin.png",
      "description": "Bitcoin description"
    },
    {
      "id": "ethereum",
      "symbol": "eth",
      "name": "Ethereum",
      "logo": "http://localhost:8000/uploads/coins/ethereum.png",
      "description": "Ethereum description"
    }
  ]
}
```

**响应字段说明**：
- `id`：币种ID（CoinGecko标准）
- `symbol`：币种符号（如：btc, eth）
- `name`：币种名称（支持自定义）
- `logo`：Logo图片URL（本地存储）
- `description`：币种描述（支持自定义）

**缓存时间**：30分钟

**测试命令**：
```bash
curl "http://localhost:8000/index.php/api/coins/list?api_key=test_api_key_123456"
```

---

### 3. 行情数据接口

**接口路径**：`GET /api/coins/quote`

**功能描述**：获取单个币种的实时行情数据

**请求参数**：
- `api_key`（必需）：批次密钥
- `coin_id`（必需）：币种ID（如：bitcoin, ethereum）
- `currency`（可选）：计价货币，默认USD

**响应示例**：
```json
{
  "code": 1,
  "msg": "获取成功",
  "time": 1766240433,
  "data": {
    "coin": {
      "id": "bitcoin",
      "symbol": "btc",
      "name": "Bitcoin",
      "logo": "http://localhost:8000/uploads/coins/bitcoin.png",
      "description": "Bitcoin description"
    },
    "quote": {
      "usd": 88000.50,
      "usd_24h_change": -0.5,
      "usd_24h_change_percentage": -0.5,
      "usd_market_cap": 1750000000000,
      "usd_24h_vol": 30000000000,
      "last_updated_at": 1766240433
    }
  }
}
```

**响应字段说明**：
- `coin`：币种基本信息
- `quote`：行情数据
  - `usd`：当前价格（USD）
  - `usd_24h_change`：24小时涨跌幅（百分比数值）
  - `usd_24h_change_percentage`：24小时涨跌幅（百分比）
  - `usd_market_cap`：市值
  - `usd_24h_vol`：24小时交易量
  - `last_updated_at`：最后更新时间戳

**缓存时间**：5分钟

**测试命令**：
```bash
curl "http://localhost:8000/index.php/api/coins/quote?coin_id=bitcoin&api_key=test_api_key_123456"
```

---

### 4. K线图接口

**接口路径**：`GET /api/coins/ohlc`

**功能描述**：获取K线数据（OHLC：开盘价、最高价、最低价、收盘价）

**请求参数**：
- `api_key`（必需）：批次密钥
- `coin_id`（必需）：币种ID
- `interval`（必需）：时间维度，可选值：`5m`（5分钟）、`1h`（1小时）、`1d`（1天）
- `currency`（可选）：计价货币，默认USD

**响应示例**：
```json
{
  "code": 1,
  "msg": "获取成功",
  "time": 1766240433,
  "data": [
    [1766240400, 88000, 88500, 87500, 88050],
    [1766240700, 88050, 88200, 87800, 88100],
    [1766241000, 88100, 88300, 87900, 88200]
  ]
}
```

**数据格式说明**：
每个数组元素格式：`[timestamp, open, high, low, close]`
- `timestamp`：时间戳（Unix时间戳）
- `open`：开盘价
- `high`：最高价
- `low`：最低价
- `close`：收盘价

**缓存时间**：
- 5分钟K线：缓存5分钟
- 1小时K线：缓存60分钟
- 1天K线：缓存1440分钟（24小时）

**测试命令**：
```bash
# 5分钟K线
curl "http://localhost:8000/index.php/api/coins/ohlc?coin_id=bitcoin&interval=5m&api_key=test_api_key_123456"

# 1小时K线
curl "http://localhost:8000/index.php/api/coins/ohlc?coin_id=bitcoin&interval=1h&api_key=test_api_key_123456"

# 1天K线
curl "http://localhost:8000/index.php/api/coins/ohlc?coin_id=bitcoin&interval=1d&api_key=test_api_key_123456"
```

---

### 5. 固件版本接口

**接口路径**：`GET /api/firmware/version`

**功能描述**：获取最新固件版本信息，支持版本比较

**请求参数**：
- `api_key`（必需）：批次密钥
- `current_version`（可选）：当前设备固件版本，用于比较是否需要更新

**响应示例**：
```json
{
  "code": 1,
  "msg": "获取成功",
  "time": 1766240433,
  "data": {
    "latest_version": "1.1.0",
    "current_version": "1.0.0",
    "need_update": true,
    "download_url": "http://localhost:8000/uploads/firmware/v1.1.0.bin",
    "file_size": 1048576,
    "release_notes": "修复了一些bug，新增了功能",
    "force_update": false
  }
}
```

**响应字段说明**：
- `latest_version`：最新版本号
- `current_version`：当前版本号（如果提供了）
- `need_update`：是否需要更新（如果提供了current_version）
- `download_url`：下载URL
- `file_size`：文件大小（字节）
- `release_notes`：发布说明
- `force_update`：是否强制更新

**缓存时间**：30分钟

**测试命令**：
```bash
# 获取最新版本
curl "http://localhost:8000/index.php/api/firmware/version?api_key=test_api_key_123456"

# 检查是否需要更新
curl "http://localhost:8000/index.php/api/firmware/version?api_key=test_api_key_123456&current_version=1.0.0"
```

---

### 6. 固件版本历史接口

**接口路径**：`GET /api/firmware/history`

**功能描述**：获取固件版本历史列表

**请求参数**：
- `api_key`（必需）：批次密钥
- `limit`（可选）：返回数量，默认10，最大50

**响应示例**：
```json
{
  "code": 1,
  "msg": "获取成功",
  "time": 1766240433,
  "data": [
    {
      "version": "1.1.0",
      "download_url": "http://localhost:8000/uploads/firmware/v1.1.0.bin",
      "file_size": 1048576,
      "release_notes": "修复了一些bug，新增了功能",
      "force_update": false
    },
    {
      "version": "1.0.0",
      "download_url": "http://localhost:8000/uploads/firmware/v1.0.0.bin",
      "file_size": 1048576,
      "release_notes": "初始版本",
      "force_update": false
    }
  ]
}
```

**缓存时间**：30分钟

**测试命令**：
```bash
curl "http://localhost:8000/index.php/api/firmware/history?api_key=test_api_key_123456&limit=10"
```

---

### 7. 配置信息接口

**接口路径**：`GET /api/config/index`

**功能描述**：获取系统配置信息

**请求参数**：
- `api_key`（必需）：批次密钥

**响应示例**：
```json
{
  "code": 1,
  "msg": "获取成功",
  "time": 1766240433,
  "data": {
    "refresh_interval": 300,
    "default_currency": "USD",
    "coingecko_api_key": "",
    "display_coins": ["bitcoin", "ethereum"],
    "timezone": "Asia/Shanghai"
  }
}
```

**配置项说明**：
- `refresh_interval`：数据刷新间隔（秒），默认300秒（5分钟）
- `default_currency`：默认计价货币，默认USD
- `coingecko_api_key`：CoinGecko API密钥（如果有）
- `display_coins`：默认显示的币种列表（币种ID数组）
- `timezone`：时区设置

**缓存时间**：1分钟

**测试命令**：
```bash
curl "http://localhost:8000/index.php/api/config/index?api_key=test_api_key_123456"
```

---

## 错误码说明

| 错误码 | HTTP状态码 | 说明 | 示例 |
|--------|-----------|------|------|
| 400 | 400 | 缺少必需参数：api_key | `{"code":0,"msg":"Missing required parameter: api_key","time":1766240433,"data":null}` |
| 401 | 401 | api_key无效 | `{"code":0,"msg":"Invalid api_key","time":1766240433,"data":null}` |
| 403 | 403 | 批次被禁用 | `{"code":0,"msg":"Batch is disabled","time":1766240433,"data":null}` |
| 429 | 429 | 请求频率超限 | `{"code":429,"msg":"Rate limit exceeded. Maximum 100 requests per minute.","time":1766240433,"data":{"rate_limit":100,"retry_after":30}}` |

---

## 限流说明

### 限流规则

- 每个批次独立限流
- 限流次数从批次表的`rate_limit`字段读取（默认100次/分钟）
- 使用缓存记录请求次数（按分钟窗口）

### 响应头

当请求通过限流检查后，响应头会包含以下信息：

```
X-RateLimit-Limit: 100
X-RateLimit-Remaining: 95
X-RateLimit-Reset: 1766240460
```

**说明**：
- `X-RateLimit-Limit`：限流上限（次/分钟）
- `X-RateLimit-Remaining`：剩余请求次数
- `X-RateLimit-Reset`：重置时间戳（下一分钟开始的时间戳）

### 超过限流

当超过限流时，返回429状态码：

```json
{
  "code": 429,
  "msg": "Rate limit exceeded. Maximum 100 requests per minute.",
  "time": 1766240433,
  "data": {
    "rate_limit": 100,
    "retry_after": 30
  }
}
```

---

## 数据同步策略

### 被动同步策略

系统采用**被动同步策略**，即：
- 当设备请求数据时，检查缓存是否过期
- 如果缓存过期或不存在，则从CoinGecko API获取最新数据
- 更新缓存并返回数据
- **不主动定时抓取数据**，避免不必要的API调用

### 手动同步命令

如果需要手动同步币种数据，可以使用以下命令：

```bash
cd web
php think sync:coins
```

该命令会：
- 从CoinGecko获取币种列表
- 同步常见币种的详细信息
- 下载并存储Logo图片

---

## 常见问题

### Q1: 为什么没有定时抓取任务？

A: 系统采用被动同步策略，当设备请求数据时自动检查缓存并更新，这样可以：
- 减少不必要的API调用
- 降低CoinGecko API的调用频率
- 提高系统响应速度（优先使用缓存）

### Q2: 如何更新币种数据？

A: 有两种方式：
1. **自动更新**：当设备请求数据时，如果缓存过期会自动更新
2. **手动更新**：使用命令 `php think sync:coins` 手动同步

### Q3: 缓存时间可以调整吗？

A: 可以，缓存时间在代码中定义：
- 币种列表：30分钟
- 行情数据：5分钟
- K线数据：根据时间维度（5m/1h/1d）
- 固件版本：30分钟
- 配置信息：1分钟

### Q4: 如何获取更多币种？

A: 在后台管理界面中：
1. 进入"币种管理"
2. 点击"添加"
3. 输入币种ID（CoinGecko标准）
4. 系统会自动从CoinGecko获取详细信息

---

**文档版本**：v1.0.0  
**最后更新**：2025-12-20

